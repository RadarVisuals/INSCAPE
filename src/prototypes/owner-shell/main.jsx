import React from 'react';
import ReactDOM from 'react-dom/client';
import '../../index.css';
import OwnerShellPrototype from './OwnerShellPrototype.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <OwnerShellPrototype />
  </React.StrictMode>,
);
