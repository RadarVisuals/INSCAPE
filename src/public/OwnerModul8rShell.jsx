import OwnerLatticeShell from './OwnerLatticeShell.jsx';

// This wrapper is the complete production owner presentation selected at the
// existing lazy owner boundary. OwnerLatticeShell remains the source rollback.
export default function OwnerModul8rShell(props) {
  return <OwnerLatticeShell {...props} ownerWorkspacePresentation="modul8r" />;
}
